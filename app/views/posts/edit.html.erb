<% provide(:title, 'Comment' ) %>

<div class="container">   
            <div class="span3">                
                    <div class="signup">
                        <div class='sup'> 
							<h1>Editing post</h1><br />

							<%= render 'form' %>

							<%= link_to 'Show', @post %> |
							<%= link_to 'Back', posts_path %>
                        </div>
                   </div>
            </div>
</div>