<% provide(:title, 'Show' ) %>

<div class="container"> 
     <div class="row">  
            <div class="span3">                
                    <div class="span10">                
                            <!-- <div class='search-container'>
                                  <%= form_tag posts_path, :method => :get do %>
                                  <%= text_field_tag :search, params[:search], :class => "search-query", :placeholder => '' %>
                                  <%= submit_tag "Search Blog", :class =>'search-button' %>
                                  <% end %>
                            </div> -->
                            <br />
							<div class='blog-container'>
								<% cache @post do%>
									    <h2>
									  	  <%= @post.title %>
									    </h2>
									  	    <h6>
									  	    	<%= @post.created_at.in_time_zone.strftime("PUBLISHED ON %a  %b %d, %Y @ %I:%M %p") %> BY
									  	    </h6> 
									  	<h4>
									  		<%= @post.name %>
									  	</h4>
									   <!--  </div> -->
		                                <hr /><br />
									         <p> 
									         	<%= @post.content.html_safe %>
									         </p>
									    <div>
									    <br /><br />
									    </div> <br />
									    <div id='blog-container2'>
									        <%= render @post.comments %> 
								<% end %>
							                <% if user_signed_in? %>
												<%= render "comments/form" %>
												<p>To embed code, please use the the 'pre' tags</p>
												<p>If you want to use the fomatting we used, insert [class='markitup'] inside the 'pre' tags.</p>
									        <% else %>
										            <p>
											  		    <%= link_to 'Log in', 'login' %> <i> to leave a comment</i>
											  	    </p>
												  	<p>
												  		<%= link_to 'Back to Posts', posts_path %> 
												 	</p>
									        <% end %>
									        <% if current_user.try(:admin?)  %>
									            <%= link_to 'Edit Post', edit_post_path(@post) %>
									        <% else %>
									        <% end %> 
									    </div>
							</div>
                        </div>
                    </div>
            </div>
     </div>
</div>


